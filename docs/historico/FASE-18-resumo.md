# Fase 18 — resumo

O contrato do projeto mudou-se para `docs/CLAUDE.md`, e na raiz ficou um `CLAUDE.md` de
duas linhas que o importa. Tinhas feito a mudança à mão; confirmei que não se perdeu um
byte e corrigi os caminhos que ela partia.

Todos os ficheiros de texto levaram uma linha vazia no fim, para a listagem do GitHub
mostrar o mesmo commit em todos. Os PDF, as imagens, as fontes e o ficheiro do domínio
ficaram intactos.

A prova de que foi mesmo só cosmético: os dois PDF do CV continuam com os mesmos bytes
de antes, e as verificações e as auditorias deram todas o mesmo resultado.

Foi tudo num só commit, como pediste — por isso o hash dele não está escrito na
documentação, só no histórico do git.

